provider:
  name: faas
  gateway: http://127.0.0.1:8080

functions:
  imageblur:
    lang: go
    handler: ./src/imageFilters/blurfilter
    image: blurfilter:latest
    environment:
      write_debug: true
        
